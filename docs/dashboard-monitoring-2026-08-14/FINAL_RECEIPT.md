# 最终回执

状态：供应商流量数据边界纠正已完成本地实现和验证，等待生产发布。

## 已完成

1. 新监控首页与七台服务器运行卡片已在生产运行。
2. 心跳 Agent 2.1 继续提供活性、CPU、内存、空间与实时 IO。
3. 套餐流量停止使用 Agent 网卡增量，只显示供应商管理画面或官方 API 读数。
4. 每台服务器的供应商读数保存独立账期、已用量、限额、来源和采集时间。
5. 版本 3 幂等迁移清空误用的 `server_traffic_meter` 数据，运行代码停止读写。表结构在发布观察期内保留以支持短期回滚。
6. 首页无供应商数据时明确显示等待状态，不再展示容易误解的 MB 数值。

## 验收

* 完整自动化测试 54 项通过。
* JavaScript 语法、Python 编译和差异格式检查通过。
* 本地浏览器验证四台卡片，其中三台等待供应商读数，一台展示独立周期的供应商样本。
* 页面汇总显示供应商数据 1/4，浏览器控制台警告和错误为 0。
* 验证截图为 `docs/assets/dashboard-provider-usage-source-20260814.png`。

## 外部资料待补充

1. 当前没有可复用的 VMISS、Riven Cloud、CCNode、AWS、CNDRP、OrangeVPS 或 Sakura 供应商后台会话。
2. 需要先取得一个供应商的授权登录资料与对应服务器服务编号，完成首个专用连接器和真实数据验收。
3. VMISS 自动化方式需要先符合其公开条款。
