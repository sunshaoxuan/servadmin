# AWSNODE-MAIN 监控停用回执

日期：2026-08-14

## 变更结果

生产节点 `AWSNODE-MAIN` 保留服务器档案和历史检查记录，生命周期改为已失效，分布式心跳开关关闭。监控首页、活动资产查询和心跳矩阵不再纳入该节点。

## 生产验证

| 检查项 | 结果 |
| --- | --- |
| 服务器编号 | `3` |
| `is_retired` | `1` |
| `heartbeat_enabled` | `0` |
| 活动服务器数量 | `7` |
| 活动心跳节点数量 | `7` |
| 默认资产列表 | `AWSNODE-MAIN` 已隐藏 |
| 监控首页 | `AWSNODE-MAIN` 卡片不存在 |
| 审计记录 | `admin` 在 2026-08-14 07:19:27 UTC 执行服务器更新 |
| 页面控制台 | 0 项警告或错误 |

## 限制

从本地工作站和 Server Desk 生产节点连接 `13.113.183.205:22` 均超时，因此未能进入 AWSNODE-MAIN 停止其本地 `server-desk-heartbeat` 服务和定时器。生产管理端已经依据活动资产过滤排除该节点，后续收到的旧 Agent 报告不会重新进入监控首页。

生产页面 DOM 已完成核验。浏览器截图接口连续三次超时，本次没有生成截图文件。

## 恢复方式

需要恢复监控时，在服务器资产中开启“显示已失效”，编辑 AWSNODE-MAIN，取消“已失效”并重新启用“分布式心跳”。SSH 恢复后检查或重启远端 Agent 服务。
