# 测试结果

## 自动测试

最终命令：`python -m pytest -q --basetemp=.tmp/pytest-final`

最终结果：60 passed in 14.02s。

`git diff --check` 通过，仅输出现有 Windows 换行转换提示。

## 浏览器测试

本地应用使用独立测试数据库和关闭外部供应商同步的配置运行。

已验证：

1. 输入 `29.260123456` 与 `8589.934592` 时，两个数值字段均为有效状态。
2. 两个字段的 step 属性均为 `any`。
3. 管理画面地址留空时，提交成功。
4. 卡片显示 `29.3 GB / 8.6 TB`。
5. 新页面控制台没有 error 或 warning。
6. 输入 OrangeVPS 名称和后台域名后，连接器为 `orangevps` 且自动同步为开启状态。

## 未执行项目

未使用真实供应商账号访问外部站点。已有连接器 mock 测试覆盖登录流程、数据解析和数据库持久化。
